# Algumas funções e sintaxe que eu vivo esquecendo rs <3

s = []
s = s[0:10] # corta a string
s = s[::-1] # inverte a string

s = s.replace("substituido", "substituicao") # substitui algo na string
s = s.lower() # tudo minúsculo
s = s.upper() # tudo maiúsculo

# LISTA:

lista = []
lista.append(n)
lista.pop(n)
len(lista) # tamanho da lista
range(len(s)) # range do tamanho da lista

# HASH:

tabela = set()
tabela.add()
